# Required for standalone APK/AAB builds.
# Use the public HTTPS domain where artifacts/api-server is deployed.
# The app automatically appends /api when it is not already present.
EXPO_PUBLIC_API_URL=https://your-api-domain.example