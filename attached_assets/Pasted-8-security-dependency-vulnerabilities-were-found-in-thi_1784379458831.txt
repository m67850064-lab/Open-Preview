8 security dependency vulnerabilities were found in this repl:

CVE-2025-48997@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2025-47935@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2026-3520@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2026-3304@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2026-5079@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2025-7338@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2025-47944@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
CVE-2026-2359@multer-1.4.5-lts.1 in multer@1.4.5-lts.1
Please fix these vulnerabilities using the following workflow:

Enumerate EVERY dependency manifest in the repl tree before doing anything else — repls commonly contain nested manifests (frontend/, backend/, apps/, packages/, services/*, etc.), and the scanner walks the entire tree. A fix that only touches the root manifest will leave nested copies vulnerable and the same finding will reappear on the next scan. Use a real filesystem walk from the repl root that PRUNES nested dependency/VCS/build-output directories (node_modules, .git, vendor, .venv, target, dist, build, etc.) — use prune-style exclusions so the walker skips traversal INTO those directories rather than filtering after the fact. Do NOT rely on git ls-files: it only reports tracked files, so an untracked nested manifest would be silently skipped.
For each manifest discovered in step 1, identify the package manager from the manifest+lockfile pair and run that ecosystem's built-in auto-fix from that manifest's directory (e.g. cd <manifest dir> && npm audit fix for npm, pnpm audit --fix for pnpm, pip-audit --fix for Python with pip). Do this per-manifest — running the auto-fix once in the repl root does NOT cascade into nested manifests, even when they declare the same package.
For ecosystems without an auto-fix command (yarn, bundler, go modules, gradle, maven, composer, nuget, etc.) or for vulnerabilities the auto-fix didn't resolve, look up the patched version from the advisory and update the manifest directly — bump the dependency if it's top-level, or update the parent or pin a version override (npm/pnpm overrides, yarn resolutions, etc.) if it's transitive — then regenerate the lockfile if the ecosystem uses one. Repeat for every manifest that pins the vulnerable version.
If no patched version exists, evaluate whether the vulnerable code path is actually reachable and consider alternative packages if needed.
Verification: after the fixes, re-check each manifest and its lockfile and confirm that none of the listed package@version pairs from the findings above are still pinned anywhere in the tree. If any nested manifest still references a vulnerable version, fix it before considering the work complete.
Ensure the application still builds and works correctly after the fix.